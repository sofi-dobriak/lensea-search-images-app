const ErrorMessage = () => {
  return (
    <p className='text-center text-[var(--error-text-color)]'>
      Whoops, something went wrong... Please try reloading this page!
    </p>
  );
};

export default ErrorMessage;
